{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "prebuild": "tailwindcss build src/index.css -o public/index.css",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-postcss": "^1.1.0",
    "@snowpack/plugin-react-refresh": "^2.4.0",
    "autoprefixer": "^10.2.4",
    "postcss": "^8.2.6",
    "postcss-cli": "^8.3.1",
    "snowpack": "^3.0.1",
    "tailwindcss": "^2.0.3"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
